syntax = "proto3";
package data;
import "src/data/uuid.proto";

message Message {
enum Intent {
    SYN = 0;
    ACK = 100;
    STATECHANGE = 200;
    BUILD = 300;
  }
  UUID uuid = 1;
  Intent type = 2;
}
